"""Exact MaxCut circuits, finite-shot search, and reproducible study artifacts.

``qaoa`` supplies the simulator and exact structural comparator; ``search``
contains priors and shot-accounted optimizers; ``experiment`` freezes and runs
the protocol; ``verify`` independently audits it; ``report`` and ``artifacts``
derive figures, statistics, and portable exports from the canonical bundle.
"""

__version__ = "0.1.0"
